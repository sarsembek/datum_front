upstream web {
    server web:3000;  # Указывает на сервис web в Docker Compose
}

server {
    listen 80;
    listen [::]:80;

    server_name example.com;  # Замените на ваш домен или IP
    server_tokens off;

    # Let's Encrypt (обеспечение сертификатов)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Прокси для Nuxt.js
    location / {
        proxy_pass http://web;  # Прокси на сервис web
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;

        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    error_page 502 /502.html;
    location = /502.html {
        root /usr/share/nginx/html;
        internal;
    }
}









# upstream web {
#     server web:3000;  # Оставляем так, если web сервис доступен через порт
# }

# server {
#     listen 80;
#     listen [::]:80;

#     server_name localhost;
#     server_tokens off;

#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }

#     location / {
#         return 404;
#     }
# }

# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;

#     server_name chatbot.datum-platform.com;

#     ssl_certificate /etc/nginx/ssl/live/chatbot.datum-platform.com/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/live/chatbot.datum-platform.com/privkey.pem;

#     location / {
#         proxy_pass http://web;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }